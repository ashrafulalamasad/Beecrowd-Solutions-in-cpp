#include<bits/stdc++.h>
using namespace std;

int main()
{
    float a, b, c; cin >> a >> b >> c;
    cout << "MEDIA = " << fixed << setprecision(1) << (a*0.2 + b * 0.3 + c * 0.5) << endl;
    return 0;
}
